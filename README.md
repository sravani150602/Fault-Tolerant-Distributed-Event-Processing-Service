# Fault-Tolerant-Distributed-Event-Processing-Service
Fault-tolerant distributed event processing service using AWS Lambda triggered by S3 events via API Gateway, processing 15K+ simulated events with retry logic and idempotent deduplication, achieving under 300ms end-to-end latency.
